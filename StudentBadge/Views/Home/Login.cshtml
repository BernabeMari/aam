<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #121212;
            font-family: 'Arial', sans-serif;
            color: #B39DDB;
        }

        .card {
            border-radius: 10px;
            background-color: #1E1E1E;
            box-shadow: 0px 4px 15px rgba(179, 157, 219, 0.3);
            color: #EDE7F6;
        }

        .form-control {
            border-radius: 5px;
            background-color: #292929;
            border: 1px solid #7E57C2;
            color: #EDE7F6;
        }

        .form-control:focus {
            border-color: #D1C4E9;
            box-shadow: 0 0 8px rgba(209, 196, 233, 0.5);
        }

        .btn-primary {
            background-color: #7E57C2;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            font-size: 1.1rem;
            transition: background-color 0.3s ease;
            color: #fff;
        }

        .btn-primary:hover {
            background-color: #5E35B1;
        }

        h2 {
            font-size: 2.5rem;
            font-weight: 600;
            color: #D1C4E9;
        }

        .form-group {
            margin-bottom: 1.5rem;
        }

        .container {
            margin-top: 50px;
            max-width: 600px;
        }

        .btn-lg {
            padding: 12px;
        }

        .alert {
            font-size: 1rem;
            border-radius: 5px;
            margin-bottom: 1rem;
        }
    </style>
</head>
<body>
    <div class="container">
        <h2 class="text-center my-4">Login</h2>
        <form method="post" action="/Account/Login">
            <div class="card shadow-lg p-4 border-0">
                <div class="card-body">
                    <!-- Display Error Message from ViewBag -->
                    @if (!string.IsNullOrEmpty(ViewBag.Error))
                    {
                            <div class="alert alert-danger text-center" role="alert">
                            @ViewBag.Error
                            </div>
                    }

                    <div class="form-group">
                        <label for="username">Username</label>
                        <input type="text" id="username" name="username" class="form-control form-control-lg" required placeholder="Enter your username" />
                    </div>
                    <div class="form-group">
                        <label for="password">Password</label>
                        <input type="password" id="password" name="password" class="form-control form-control-lg" required placeholder="Enter your password" />
                    </div>
                    <div class="text-center">
                        <button type="submit" class="btn btn-primary btn-lg mt-4" style="width: 100%;">Login</button>
                    </div>
                    <div class="text-center mt-3">
                        <p>Don't have an account? <a href="/Home/Signup" class="text-decoration-none" style="color: #7E57C2;">Sign Up here</a></p>
                    </div>
                </div>
            </div>
        </form>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
